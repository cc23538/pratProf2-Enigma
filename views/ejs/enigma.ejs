<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enigma</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f4f8;
            margin: 0;
            padding: 0;
            color: #333;
        }

        h1 {
            background-color: #3498db;
            color: #fff;
            padding: 20px;
            text-align: center;
            margin: 0;
        }

        table {
            width: 60%;
            margin: 20px auto;
            border-collapse: collapse;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            overflow: hidden;
        }

        th, td {
            border: 1px solid #3498db;
            padding: 12px;
            text-align: left;
        }

        th {
            background-color: #2980b9;
            color: #fff;
        }

        tbody tr:nth-child(even) {
            background-color: #ecf0f5;
        }

        form {
            width: 60%;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        form label,
        form input {
            width: calc(48% - 10px);
            margin-bottom: 10px;
            box-sizing: border-box;
        }

        form input {
            padding: 10px;
            border: 1px solid #3498db;
            border-radius: 4px;
        }

        form button {
            width: 30%;
            padding: 10px;
            background-color: #3498db;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            align-self: flex-end;
        }

        form button:hover {
            background-color: #2980b9;
        }

        p {
            text-align: center;
            color: #3498db;
            margin: 20px 0;
        }
    </style>
</head>
    <body>
        <h1>Frases Criptografadas</h1>
        <table>
            <tr>
                <th>ID</th>
                <th>Texto Descriptografado</th>
            </tr>
        </thead>
        <tbody>
            <% for (var frase of frases) { %>
                <tr>
                    <td><%= frase.id %></td>
                    <td><%= frase.texto_criptografado %></td>
                </tr>
            <% } %>
        </tbody>
        </table>

    </body>
    
    </html>
    
</body>

</html>
